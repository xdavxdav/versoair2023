# db/update_schema.sql.txt

Use this file to paste the SQL statements or a plain-language description
of the schema changes you want applied.

Instructions:
- If you have raw SQL, paste it below between the === markers.
- If you prefer to describe changes, list them (table name, columns to add/modify).
- When ready, reply "apply" and I will run the SQL in `db/update_schema.sql`.

=== Paste SQL here ===

-- Example:
-- CREATE TABLE customers (
--   id SERIAL PRIMARY KEY,
--   email TEXT UNIQUE NOT NULL,
--   created_at timestamptz DEFAULT now()
-- );

=== End SQL ===

Security reminder: I will only run SQL after you explicitly confirm. If you
want me to preview the composed SQL before running, say "preview" instead.
